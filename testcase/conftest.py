import pytest
import yaml


# @pytest.fixture(scope="session", autouse=True)
# def parse_yaml(self, path):
#     with open(path, encoding="utf-8") as f:
#         data = yaml.load(f)
#         return data
